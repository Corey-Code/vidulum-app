# Vidulum App

Vidulum is a multi-chain wallet supporting Cosmos SDK, EVM, Solana, and UTXO networks.

## Development

```bash
npm ci
npm run dev:web
npm test
npm run lint
```

See `docs/` for guides on adding chains and network types.

## Security

Never commit seeds, private keys, or signing secrets. See `CONTRIBUTING.md`.
