# Adding a Cosmos Chain

1. Add the chain metadata to the registry data file.
2. Run `npm run sync:chains` to regenerate `src/lib/assets/chainRegistry.ts`.
3. Add tests covering the new chain's config.
