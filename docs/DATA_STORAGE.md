# Data Storage

All sensitive data (seeds, keys) is stored locally using encrypted browser storage and never leaves the device. Non-sensitive preferences and cached chain data are stored in `chrome.storage.local` (extension) or `localStorage` (web).
