# Adding a Solana (SVM) Chain

1. Add cluster endpoints and program metadata to `src/lib/assets/chainRegistry.ts`.
2. Ensure address validation uses base58 checks.
3. Add tests for SPL token balance queries.
